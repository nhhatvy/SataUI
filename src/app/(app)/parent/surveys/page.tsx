import { ParentSurvey } from '@/features/parent/survey/parent-survey'

// Danh sách đợt khảo sát trung tâm đang mở (ParentSurvey tự render list khi không có defaultSurveyId).
export default function ParentSurveysPage() {
  return <ParentSurvey />
}
