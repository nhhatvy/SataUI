import { StudentTeacherReview } from '@/features/student/teacher-review/student-teacher-review'

// Học sinh đánh giá giáo viên (form builder render động).
export default function EvaluateTeacherPage() {
  return <StudentTeacherReview />
}
