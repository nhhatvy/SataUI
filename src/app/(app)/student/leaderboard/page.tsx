import { StudentLeaderboard } from '@/features/student/leaderboard/student-leaderboard'

// Cổng Học sinh: bảng xếp hạng lớp theo điểm thành tích từ bài tập & kiểm tra.
export default function StudentLeaderboardPage() {
  return <StudentLeaderboard />
}
